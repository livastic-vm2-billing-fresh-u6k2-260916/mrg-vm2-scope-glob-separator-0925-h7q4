scopes:
  source:
    files:
      protected:
        include:
          - "**/*"
        exclude:
          - "*.md"

pull_request_rules:
  - name: VM2 scope glob separator protected merge H7Q4
    conditions:
      - title = VM2_SCOPE_GLOB_SEPARATOR_H7Q4
      - check-success = scope-report-ready
    actions:
      merge:
        method: merge
